# media.py - Código da Fase Red (Incrementado)
def calcular_media(nota1, nota2, nota3):
    # Implementação incorreta: realizando operações erradas para garantir que os testes falhem
    # Para simular um erro, faremos uma operação que não representa a média real
    soma = nota1 + nota2 + nota3
    resultado_incorreto = soma * 2  # Multiplicando a soma por 2 em vez de dividir por 3
    return resultado_incorreto
